# ========================================================================
# Apache HTTPD — FULLY FIXED CONFIG FOR PHP + FlightPHP + Swagger (macOS)
# Generated for user "macbook" — 2025
# ========================================================================

ServerRoot "/opt/homebrew/opt/httpd"

Listen 8080

# ========================================================================
# MODULES
# ========================================================================

LoadModule mpm_prefork_module lib/httpd/modules/mod_mpm_prefork.so
LoadModule authn_file_module lib/httpd/modules/mod_authn_file.so
LoadModule authn_core_module lib/httpd/modules/mod_authn_core.so
LoadModule authz_host_module lib/httpd/modules/mod_authz_host.so
LoadModule authz_groupfile_module lib/httpd/modules/mod_authz_groupfile.so
LoadModule authz_user_module lib/httpd/modules/mod_authz_user.so
LoadModule authz_core_module lib/httpd/modules/mod_authz_core.so
LoadModule access_compat_module lib/httpd/modules/mod_access_compat.so
LoadModule auth_basic_module lib/httpd/modules/mod_auth_basic.so
LoadModule reqtimeout_module lib/httpd/modules/mod_reqtimeout.so
LoadModule filter_module lib/httpd/modules/mod_filter.so
LoadModule mime_module lib/httpd/modules/mod_mime.so
LoadModule log_config_module lib/httpd/modules/mod_log_config.so
LoadModule env_module lib/httpd/modules/mod_env.so
LoadModule setenvif_module lib/httpd/modules/mod_setenvif.so
LoadModule version_module lib/httpd/modules/mod_version.so
LoadModule unixd_module lib/httpd/modules/mod_unixd.so
LoadModule status_module lib/httpd/modules/mod_status.so
LoadModule autoindex_module lib/httpd/modules/mod_autoindex.so
LoadModule dir_module lib/httpd/modules/mod_dir.so
LoadModule alias_module lib/httpd/modules/mod_alias.so
LoadModule rewrite_module lib/httpd/modules/mod_rewrite.so
LoadModule headers_module lib/httpd/modules/mod_headers.so

# ========================================================================
# PHP MODULE (THE MOST IMPORTANT PART)
# ========================================================================
# ✔ This makes PHP execute instead of downloading as text
# ✔ Verified PHP 8.4 Homebrew location

LoadModule php_module /opt/homebrew/opt/php/lib/httpd/modules/libphp.so

# ========================================================================
# SECURITY
# ========================================================================

User _www
Group _www

# ========================================================================
# ROOT ACCESS SETTINGS
# ========================================================================

<Directory />
    AllowOverride none
    Require all denied
</Directory>

# ========================================================================
# DOCUMENT ROOT — your backend/public folder via symlink to /var/www
# ========================================================================

DocumentRoot "/opt/homebrew/var/www"

<Directory "/opt/homebrew/var/www">
    Options FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

# ========================================================================
# DIRECTORY INDEX (PHP MUST COME FIRST)
# ========================================================================

<IfModule dir_module>
    DirectoryIndex index.php index.html
</IfModule>

# ========================================================================
# FILE ACCESS RESTRICTIONS
# ========================================================================

<Files ".ht*">
    Require all denied
</Files>

# ========================================================================
# LOGGING CONFIG
# ========================================================================

ErrorLog "/opt/homebrew/var/log/httpd/error_log"
LogLevel warn

<IfModule log_config_module>
    LogFormat "%h %l %u %t \"%r\" %>s %b" common
    CustomLog "/opt/homebrew/var/log/httpd/access_log" common
</IfModule>

# ========================================================================
# CGI (not used, but kept default)
# ========================================================================

<Directory "/opt/homebrew/var/www/cgi-bin">
    AllowOverride None
    Options None
    Require all granted
</Directory>

# ========================================================================
# MIME TYPES + PHP HANDLER
# ========================================================================

<IfModule mime_module>

    TypesConfig /opt/homebrew/etc/httpd/mime.types

    # Support PHP scripts
    AddType application/x-httpd-php .php
    AddHandler application/x-httpd-php .php

    AddType application/x-compress .Z
    AddType application/x-gzip .gz .tgz

</IfModule>

# ========================================================================
# SSL PLACEHOLDER
# ========================================================================

<IfModule ssl_module>
SSLRandomSeed startup builtin
SSLRandomSeed connect builtin
</IfModule>

# ========================================================================
# END OF FILE
# ========================================================================
